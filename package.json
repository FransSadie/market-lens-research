{
  "name": "market-lens-research",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "start": ".\\.venv\\Scripts\\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload",
    "backend": "npm run start",
    "frontend": "npm --prefix frontend run dev"
  }
}
